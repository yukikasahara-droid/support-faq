# 計測（検索・クリック・役立ち度）の送信先。設定すると track() が有効になる。
#   ローカル: http://127.0.0.1:3001/events
#   本番   : https://telemetry.example.com/events  （HTTPS 必須：サイトがHTTPSのため）
# 未設定なら送信しない（＝公開サンプルは無変更）。
PUBLIC_TELEMETRY_ENDPOINT=
